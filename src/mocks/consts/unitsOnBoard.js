import { myId, enemyId } from "mocks/consts/players";

const unitsOnBoard = [
  { id: 1, championId: 1, slotId: "1", playerId: myId },
  { id: 4, championId: 1, slotId: "121", playerId: enemyId },
];

export default unitsOnBoard;
